import com.wf.psrm.domain.CustomRules;
global com.wf.psrm.domain.RulesList rulesList;

dialect "mvel"

rule "Throttle Value based rule"
	when
		CustomRules(throttleValue)
	then
		rulesList.setList("Throttling is On")
end